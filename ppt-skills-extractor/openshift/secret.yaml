apiVersion: v1
kind: Secret
metadata:
  name: ppt-agent-secrets
  labels:
    app: ppt-agent
type: Opaque
stringData:
  # Replace with actual key before applying.
  # For in-cluster models (LLM_API_BASE set) this can be left empty or omitted.
  OPENAI_API_KEY: "sk-replace-me"
  # ANTHROPIC_API_KEY: ""
  # AZURE_API_KEY: ""
