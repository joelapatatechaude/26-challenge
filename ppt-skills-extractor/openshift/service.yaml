apiVersion: v1
kind: Service
metadata:
  name: ppt-agent
  labels:
    app: ppt-agent
spec:
  selector:
    app: ppt-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
